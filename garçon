n = int(input())
copos_quebrados = 0
for i in range(n): 
    L, C = (int(w) for w in input().split())
    if L > C:
        copos_quebrados += C
    else: 
        copos_quebrados += 0
print(copos_quebrados)